import InnerPage from "@/components/InnerPage";

export default function EventPlanningPage() {
  return (
    <InnerPage title="Event Planning" eyebrow="Services">
      <p>
        Timeline, vendors, family rituals and guest flow — held together so the
        day feels effortless.
      </p>
    </InnerPage>
  );
}
