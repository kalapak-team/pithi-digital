import InnerPage from "@/components/InnerPage";

export default function EntertainmentPage() {
  return (
    <InnerPage title="Entertainment" eyebrow="Services">
      <p>
        Traditional performance, live music and DJs — sequenced so the
        ceremony stays sacred and the party can still lift.
      </p>
    </InnerPage>
  );
}
